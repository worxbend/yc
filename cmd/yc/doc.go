// Package main builds the yc command-line application.
//
// The executable delegates command parsing and runtime wiring to internal
// packages so the entrypoint stays small and the behavior remains testable.
package main
